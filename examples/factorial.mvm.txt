        push 6      ; init loop: [max, i, prod]
        push 1
        push 1

floop:  dup 1       ; sum *= i
        mul

        swp         ; i++
        push 1
        add
        swp

        dup 2       ; i <= max ?
        dup 2
        sub
        bnz floop
